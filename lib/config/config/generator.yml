generator:
  class: sfHadoriThemeGenerator
  param:
    model_class:           ~
    theme:                 hadori
    non_verbose_templates: true
    with_show:             true
    singular:              ~
    plural:                ~
    route_prefix:          ~
    sortable:              false
    with_doctrine_route:   true
    actions_base_class:    sfActions
    use_security_yaml_credentials: true

    config:
      list:
        title:            %%model_class%% List
        display:          true
        actions:
          _new:           ~
          _export:        ~
        object_actions:
          _show:          ~
          _edit:          ~
          _delete:        ~
        batch_actions:
          _delete:        ~

      edit:
        title:            'Edit [?php echo $%%singular_name%% ?]'
        actions:
          _cancel:        ~
          _delete:        ~
          _save:          ~

      new:
        title:            New %%class_label%%
        actions:
          _cancel:        ~
          _save:          ~
          _save_and_add:  ~

      show:
        title:            '[?php echo $%%singular_name%% ?]'
        display:          true
        actions:
          _edit:          ~

      export:
        title:            Export %%class_label%%
        display:          true
        actions:
          _cancel:        ~

      filter:
        fields:           []

      form:
        fields:           []

      actions:            ~

      fields:             ~
